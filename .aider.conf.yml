# Aider AI Configuration — Auto-generated by claude-setup
# These settings apply when using Aider (https://aider.chat)
# Place this file in your project root or home directory

# Model preferences (Aider supports Claude, GPT, Gemini, etc.)
# Uncomment the model you want to use:

# Claude (recommended — best for code)
# model: anthropic/claude-sonnet-4-20250514

# GPT-4o
# model: openai/gpt-4o

# Gemini
# model: gemini/gemini-1.5-pro-latest

# Auto-commits after each change
auto-commits: true

# Show diffs after each change
show-diffs: true

# Use dark mode for terminal output
dark-mode: true

# Lint after each edit (uncomment and set your linter)
# lint-cmd: "npx eslint --fix"

# Test command (uncomment and set your test runner)
# test-cmd: "npm test"

# Files to always include in context
# read:
#   - CLAUDE.md
#   - README.md
#   - tsconfig.json

# Map the whole repo for better context
map-tokens: 1024

# Git settings
attribute-author: true
attribute-committer: false

# Editor settings
edit-format: diff
stream: true
